# Đường dẫn kết nối API tới Backend.
# Khi chạy cục bộ (local), cổng mặc định của Backend là http://localhost:5000.
# Khi deploy lên môi trường Production (như Render), hãy thay thế bằng URL do Render cung cấp.
# LƯU Ý: Biến môi trường trong Vite BẮT BUỘC phải bắt đầu bằng tiền tố VITE_
VITE_API_URL=http://localhost:5000
